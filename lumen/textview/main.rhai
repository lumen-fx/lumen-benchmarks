// textview bench app - the 5,000 wrapped paragraphs live directly in
// main.lmn (generated by harness/gen_corpus.py from the shared corpus;
// Lumen's Rhai API has no file I/O, so the corpus ships inside the
// markup). Nothing reactive to set up; scrolling is driven externally
// by the harness over MCP wheel events, like the list app.

fn on_start() {}
