[package]
name = "bench-slint-hello"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = { workspace = true }
bench-slint-common = { path = "../common" }

[build-dependencies]
slint-build = { workspace = true }
