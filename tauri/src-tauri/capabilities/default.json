{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Bench app capability: core defaults for the main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
